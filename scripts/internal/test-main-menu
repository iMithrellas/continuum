#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
GODOT="${GODOT:-godot}"
"$GODOT" --headless --path "$ROOT/client/godot" --scene res://tools/main_menu_test.tscn
