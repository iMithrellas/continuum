#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
GODOT="${GODOT:-godot}"
"$GODOT" --headless --path "$ROOT/client/godot" --script res://tools/local_server_runner_test.gd
